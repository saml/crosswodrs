# Quickstart

    virtualenv venv
    . venv/bin/activate
    pip install -r requirements.txt
    mkdir data
    python server.py

Download some `*.puz` files from the web and put them into `data/` directory.
